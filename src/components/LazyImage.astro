---
interface Props {
  src: string;
  alt: string;
  class?: string;
  aspectRatio?: string;
  parallaxSpeed?: number;
  contain?: boolean;
  eager?: boolean;
  depthTarget?: boolean;
}

const {
  src,
  alt,
  class: className = "",
  aspectRatio = "4 / 5",
  parallaxSpeed,
  contain = false,
  eager = false,
  depthTarget = true,
} = Astro.props;

const hasParallax = parallaxSpeed !== undefined && parallaxSpeed > 0;
---

<div
  class:list={[
    "lazy-media",
    depthTarget && "depth-target",
    contain && "lazy-media--contain lazy-media--fit",
    eager && "is-loaded",
    hasParallax && "lazy-media--parallax",
  ]}
  data-lightbox
  style={contain ? undefined : `--lazy-aspect: ${aspectRatio}`}
>
  {hasParallax ? (
    <div class="parallax-layer lazy-media__parallax" data-parallax-speed={parallaxSpeed}>
      {eager ? (
        <img
          src={src}
          alt={alt}
          class={className}
          decoding="async"
          fetchpriority="high"
        />
      ) : (
        <img
          data-src={src}
          alt={alt}
          class={className}
          decoding="async"
          loading="lazy"
        />
      )}
    </div>
  ) : eager ? (
    <img
      src={src}
      alt={alt}
      class={className}
      decoding="async"
      fetchpriority="high"
    />
  ) : (
    <img
      data-src={src}
      alt={alt}
      class={className}
      decoding="async"
      loading="lazy"
    />
  )}
</div>
