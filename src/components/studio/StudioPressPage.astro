---
import StudioLayout from '../../layouts/StudioLayout.astro';
import { pressLinks } from '../../data/content';
import { t, type LocaleKey } from '../../data/translations';

interface Props {
  locale?: LocaleKey;
}

const { locale = 'en' } = Astro.props;
const l = t[locale];
---

<StudioLayout title={`${l.nav.press} — Anna Dauyl Rockswell`} active="press" seoPage="press" locale={locale}>
  <section class="st-page">
    <header class="st-page-head st-reveal">
      <h1 class="st-display">{l.nav.press}</h1>
    </header>

    <div class="st-index" data-reveal-group>
      {pressLinks.map((item) => (
        <a href={item.href} target="_blank" rel="noopener noreferrer" class="st-index__row st-reveal">
          {item.platform ? <span class="st-index__meta">{item.platform}</span> : null}
          <span class="st-index__title">{item.title}</span>
        </a>
      ))}
    </div>
  </section>
</StudioLayout>
